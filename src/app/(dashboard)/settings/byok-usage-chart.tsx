import type { ByokPoint } from "@/lib/usage/queries";

/**
 * Stub — Task 10 replaces this body with the BYOK sweep-token chart. Kept
 * here with the final prop signature so `usage-settings.tsx` can wire it up
 * now and Task 10 only has to fill in the render.
 */
export function ByokUsageChart(props: { monthToDate: number; points: ByokPoint[] }) {
  return null;
}
